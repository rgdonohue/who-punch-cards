
var app = (function(parent){
  // start up the app!

  parent.init = function() {
      app.processData.init();
//      app.chart.init();
//      app.chart.parseData();

  }

  return parent;

})(app || {});
